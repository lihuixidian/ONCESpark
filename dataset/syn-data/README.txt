Sender.scala
The script for generating synthetic sequence, where the alphabet set as well as the length for the sequence can both be configed in the code.
We use this script to generate the three large sequences, namely Synthetic-Spark-1, 2 and 3, respectively.

